apiVersion: v1
kind: Service
metadata:
  name: llm-katan
spec:
  type: ClusterIP
  selector:
    app: llm-katan
    app.kubernetes.io/name: llm-katan
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP

